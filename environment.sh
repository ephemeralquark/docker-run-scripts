#! /bin/bash

# GCP Cloud SQL service credential in json form to use with cloud sql proxy
export CLOUD_SQL_INSTANCE_CREDENTIALS_PATH=#[Replace with path to service credential.json here]
# GCP Cloud SQL instace name
export CLOUD_SQL_INSTANCE_NAME=#[Replace with cloud sql instance name here]